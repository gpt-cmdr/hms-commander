# Agent Instructions

This directory contains Claude preload helpers and detailed domain notes.

**Non-Claude agents**: Use root `AGENTS.md` and local subtree `AGENTS.md` files as the shared source of truth. Read specific rule files here only when deeper domain context is needed.

Domains: `hec-hms/`, `python/`, `testing/`, `integration/`, `project/`, `documentation/`, `workflow/`.
