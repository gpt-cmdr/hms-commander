# Agent Instructions

This directory is Claude-native infrastructure.

**Non-Claude agents**: Use root `AGENTS.md` as the shared source of truth. Read specific files here only when the task explicitly involves Claude-native agents, commands, rules, or skills.

`CLAUDE.md`, rules, agents, skills, and commands in this directory are Claude preload helpers and adapters. They are not the canonical shared policy tree.
